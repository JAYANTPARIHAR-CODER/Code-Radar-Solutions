#include <stdio.h>


int main() {
    char n;
    scanf("%c",&n);
    if((ch>="A") && (ch<="Z")){
        printf("Uppercase");
    }
    else{
        printf("Lowercase");
    }

    return 0;
}